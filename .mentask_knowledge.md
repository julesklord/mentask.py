# Project Knowledge: app

## Project Patterns

-

## Fixed Errors

-

## Tech Stack

-

## Lessons Learned & Facts

- To unit test an inner function like `code_replacer` inside `ContextCompressor.smart_compress` using `re.sub`, extract it to a class method (`@classmethod def _code_replacer(cls, match)`) to make it independently testable without altering runtime behavior. Pass a mock match object (implementing a `group(index)` method) to thoroughly test the happy path and edge cases.

## Last Updated: 2026-05-10
